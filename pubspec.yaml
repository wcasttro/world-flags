name: world_flags
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  flutter_svg: ^2.0.9
  google_mobile_ads: ^5.2.0
  firebase_core: ^4.0.0
  firebase_crashlytics: ^5.0.0
  shared_preferences: ^2.2.2
  vibration: ^1.9.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/translations/
    - assets/flags_data.json
    - assets/country_hints.json
    - assets/logo.png
  